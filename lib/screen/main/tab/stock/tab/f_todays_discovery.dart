import 'package:flutter/material.dart';

class TodaysDiscoveryFragment extends StatelessWidget {
  const TodaysDiscoveryFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
